# 2025-07-17

- 扩展 `scripts/optimize-images.mjs`：通过 HTTP 拉取 R2 卡面 PNG + sharp 压缩为 webp 缩略图 + wrangler r2 上传到 `cards/thumb/`
- optimize 从 build 流程独立出来，避免每次 build 都跑大量网络操作
- 下载按钮优化：背面不显示，正面仅 hover 时显示（opacity 过渡）
- 修复下载按钮不显示 bug：移除 `frontDownload !== frontSrc` 前置条件，无缩略图时原图直接作为预览+下载源
- 修复 wrangler r2 object put 缺少 `--remote` 参数导致缩略图只上传到本地存储而非远程 R2
- Guide 提示词新增【签名】占位符，签名单独一行，用户可自定义右下角签名文字
