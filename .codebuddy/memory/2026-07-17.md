# 2026-07-17

## R2 Bucket 改名
- 将 R2 bucket 从 `sucards-images` 改为 `sucards-images-apac`
- `wrangler.toml` 已经是最新的，更新了 `scripts/optimize-images.mjs` 中的 `BUCKET` 常量
- `scripts/migrate-r2.mjs` 是迁移脚本，已引用两个 bucket 名，无需改动

## 下载按钮位置调整
- 从卡面正面右下角浮动（遮挡图片）移到卡片下方编号旁边
- 新增 `.card-footer` flex 容器包裹编号 + 下载按钮
- 下载按钮改为 inline pill 样式，半透明，hover 高亮
